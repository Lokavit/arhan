import { text } from "./text";
import { hms } from "./date";
import { percent, numer, crypto, amount, quantity } from "./number";

const fmt = {
  text,
  hms,
  percent,
  numer,
  crypto,
  amount,
  quantity,
};

export default fmt;
